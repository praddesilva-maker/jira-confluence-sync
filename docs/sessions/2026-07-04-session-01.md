# Session 01 — 2026-07-04

**Goal:** Phase 0 scaffold (repo + docs skeleton + Forge app scaffold), per
`prompts/phase-0-scaffold.md`; then an approved mid-session change request, CR-001
(configurable hierarchy root).

**Done:**
- Forge app hand-scaffolded (no Atlassian credentials available to the agent, so `forge create`
  wasn't usable): `jira:globalPage` Custom UI (React + Vite), one `ping` resolver, `storage:app`
  scope only. TypeScript strict, Vitest (1 passing test), ESLint 9 + Prettier, npm scripts
  dev/build/test/lint/typecheck — all clean.
- Full docs skeleton per `docs/documentation-strategy.md`: `CLAUDE.md`, `docs/STATE.md`, design/
  ADR/delivery stubs, `docs/delivery/phase-plan.md` (7 phases).
- ADR-001 (Custom UI global page), ADR-002 (asUser identity — pulled forward from Phase 1 to
  resolve an ADR-numbering collision flagged to the user before writing anything), ADR-003 (Forge
  LLMs API primary provider, renumbered from the original prompt's "ADR-002").
- GitHub repo created (`praddesilva-maker/jira-confluence-sync`, public) and PR #1 opened ("Phase
  0: scaffold + docs skeleton"), CI green.
- Fixed a real deploy blocker the user hit running the manual steps: `manifest.yml`'s resolver
  `handler` must omit the `src/` prefix (Forge resolves handlers relative to an implicit `src/`
  root) — was `index.handler`, then wrongly `src/resolvers/index.handler`, now correctly
  `resolvers/index.handler`. `forge login`/`forge register` completed by the user; `forge lint`
  now passes with no issues.
- **CR-001** (approved mid-session): hierarchy root made configurable per pair
  (`initiative | feature | epic`), reversing Q1's hard Jira Premium assumption. Wrote ADR-004,
  reversed §13 Q1 in place, updated drift statuses (`OUT_OF_SCOPE`), Confluence adapter page-model
  variants, portability/storage sections, and extended `prompts/phase-1-config.md` +
  `docs/delivery/phase-plan.md` (Phases 1–2) accordingly. PR #2 opened against `phase-0/scaffold`
  (branches off it since #1 isn't merged), CI green.
- Follow-up consistency sweep (approved same-branch extension of CR-001): rewrote §1 Purpose, §5.1
  Jira adapter, §7 review board, and two more spots the initial pass missed (§6 pipeline, Q6 — both
  assumed Feature-page fan-out unconditionally); renamed the `InitiativePair` working type to
  `PairConfig`; added a new `CLAUDE.md` standing order requiring this kind of sweep on future
  decision-reversing PRs.
- Filed [#3](https://github.com/praddesilva-maker/jira-confluence-sync/issues/3) for a
  non-blocking CI deprecation warning (`actions/checkout@v4`/`actions/setup-node@v4` on Node 20).

**Blocked/parked:**
- Issue #3 (GitHub Actions Node 20 deprecation warning) — non-blocking, parked for a future
  workflow bump.
- **No code verification against the live Atlassian site has happened yet.** PR #1 and PR #2 are
  both still open/unmerged. The agent has no Atlassian credentials in this environment — all
  `forge deploy`/`forge install`/site verification must be run manually by the user.

**Next session should start with:** merge PR #1, then PR #2 (after it auto-retargets to `main`),
then run the Phase 0 manual verification steps in `docs/STATE.md` ("In flight / next 1–3 steps") —
`forge deploy` + `forge install` + confirm the global page renders on
`pradeep-de-silva.atlassian.net` per `docs/delivery/test-notes.md#phase-0`. Only after that should
Phase 1 start, via `prompts/phase-1-config.md`.
