# ADR-NNN: <Title>

**Status:** Proposed | Accepted | Superseded by ADR-0NN
**Date:** YYYY-MM-DD

## Context

What decision are we facing, and what are the real alternatives? Keep this to the facts that
constrain the choice — link to the relevant `solution-architecture.md` section instead of
repeating it.

## Decision

The one or two sentences that state what we chose.

## Consequences

What this makes easier, what it makes harder, and what it forecloses. Include the "escape hatch"
if one exists.

---

ADRs are append-only. If a later decision supersedes this one, add a `Superseded by ADR-0NN` line
to the Status field above — never delete or rewrite history.
